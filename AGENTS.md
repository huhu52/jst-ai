# 机设通 AI 开发规则

本项目是一个可真实上线的静态 H5 产品，所有后续开发必须遵守以下规则。

## 技术边界

- 只使用 HTML5 + TailwindCSS CDN + 原生 JavaScript。
- 不使用 Vue、React、Angular 或其他前端框架。
- 不使用 Node 构建流程，不引入打包工具。
- 不做后台系统。
- 不做登录注册。
- 不做数据库。
- 不接入复杂 AI 接口。
- 所有设备资料和 FAQ 均使用本地 JSON 文件维护。

## 产品边界

- 面向高校机械实验设备扫码说明场景。
- 移动端优先，必须适配手机浏览器和微信内置浏览器。
- 首页和设备页都必须保留安全免责声明。
- 涉及设备启动、控制参数修改、维修、异常处理等内容时，必须提示联系教师或实验员。

## UI 规则

- 页面风格为深色工业科技风。
- 主色使用科技蓝或青色。
- 安全提醒使用橙色、黄色或红色强调。
- 页面应简洁易读，按钮适合触控。
- 不做后台管理风格页面。

## 数据规则

- 设备数据维护在 `data/devices.json`。
- FAQ 数据维护在 `data/faq.json`。
- 设备详情页通过 `device.html?id=设备ID` 读取设备。
- 新增设备时必须保证 `id` 唯一。

## 当前阶段

当前是第一阶段项目骨架：

- 首页入口已建立。
- 设备详情基础页面已建立。
- 设备数据和 FAQ 示例已建立。
- 安全确认弹窗、localStorage 和问答匹配将在下一阶段开发。
